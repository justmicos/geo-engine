# Contributing to GEOEngine

1. Fork the repository
2. Create a feature branch
3. Run `make test && make privacy-check` before committing
4. Open a Pull Request

Never commit credentials, local paths, or personal information.
